<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 검토 : 각 sql id 이름 CRUD 중심으로 바꾸기 -->
<mapper namespace="com.nki.t1.dao.UsersMapper">
    <select id="selectUserById" parameterType="UserDto" resultType="UserDto">
        select uno as uno,
               id as id,
               password as password,
               birth as birthday,
               created_at as createdAt,
               updated_at as updatedAt
        from users
        where id = #{id}
    </select>

    <select id="findById" parameterType="String" resultType="UserDto">
        select uno as uno,
               id as id,
               password as password,
               birth as birthday,
               role as  userRole,
               created_at as createdAt,
               updated_at as updatedAt
        from users
        where id = #{value}
    </select>

    <select id="idChk" parameterType="String" resultType="int">
        select count(*)
        from users
        where id = #{value}
    </select>

    <select id="existMember" parameterType="UserDto" resultType="boolean">
        select count(*)
        from users
        where id = #{id}
    </select>

    <insert id="insertUser" parameterType="UserDto">
        <selectKey keyProperty="uno" order="BEFORE" resultType="int">
            select ifnull(max(uno), 0) + 1 as new_uno from users
        </selectKey>
        insert into users (uno, id, password, role)
        values (#{uno}, #{id}, #{password}, #{userRole})
    </insert>
</mapper>